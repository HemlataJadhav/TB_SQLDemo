git clone <repository path> ==="clone the repo"
git branch "branch name" ==="for creating new branch"
git checkout "branch name"==="swiitching the branch"
git add <filename> ==="for adding files"
git add==="for adding all files "
git add <foldername>==="for adding folder"
git commit -m "messege"==="for commiting"
git push -u origin <branch name>==="for push the changes into repo"
git pull==="take latest changes"
